% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DGIdbr.R
\name{run_gene_set}
\alias{run_gene_set}
\title{Run DGIdb query for one gene set and write CSV outputs}
\usage{
run_gene_set(
  label,
  genes,
  out_dir,
  approve = TRUE,
  enrichment = TRUE,
  background_N = NULL,
  gene_direction = NULL
)
}
\arguments{
\item{label}{Label for the gene set (used in logs/output paths)}

\item{genes}{Character vector of gene symbols}

\item{out_dir}{Output directory; created if missing}

\item{approve}{Logical; if TRUE keep only FDA approved drugs; if FALSE keep all.}

\item{enrichment}{Logical; if TRUE compute hypergeometric enrichment
p-values and FDR. Requires additional API calls per drug. Default TRUE.}

\item{background_N}{Integer or NULL; number of background genes for
hypergeometric test. If NULL (default), automatically queries DGIdb
for the total druggable gene count (~11665). Set a number to override.}

\item{gene_direction}{Optional named character vector mapping gene symbols
to \code{"up"} or \code{"down"}. When provided, ChEMBL is queried for
drug mechanism of action and the consistency between drug action direction
(inhibitor/suppressor vs. activator/enhancer) and gene expression direction
is scored. Added columns: \code{n_with_direction_data},
\code{n_direction_consistent}, \code{direction_ratio} in the output.
Default \code{NULL} (skip direction analysis).}
}
\value{
Invisibly returns NULL; writes \code{dgidb_hits.csv} and \code{dgidb_raw.csv}
}
\description{
Run DGIdb query for one gene set and write CSV outputs
}
